experiment:
  name: object_a_frames_medium
  command: prepare-object
  stage: M2_R2

tools:
  ffmpeg: ffmpeg

inputs:
  video_path: data/scene/object_a/raw/object_a.mp4

paths:
  image_dir: data/scene/object_a/frames_medium

frame_extraction:
  fps: 2

logging:
  enabled: false
  backend: wandb
  project: cvhw3-task1
  mode: offline
